# =============================================================================
# Penguin Protocol — Deployment Environment
# Copy to .env and fill in values. Never commit .env.
# =============================================================================

# ── Signer ────────────────────────────────────────────────────────────────────
# Deployer private key (hex, no 0x prefix).
# This wallet pays gas and is the initial factory owner.
PRIVATE_KEY=

# ── Network ───────────────────────────────────────────────────────────────────
# Alchemy API key — used for BNB Chain mainnet RPC (bnb-mainnet.g.alchemy.com)
ALCHEMY_API_KEY=

# BscScan API key for contract verification (free at bscscan.com/apis).
BSCSCAN_API_KEY=

# ── Payment Token (USDC / USDT on BSC) ───────────────────────────────────────
# ⚠️  BSC stablecoins use 18 decimals, NOT 6.
#     All price/amount env vars below must be in 18-decimal units.
#
# Common choices:
#   Binance-Peg USDT (18 dec): 0x55d398326f99059fF775485246999027B3197955
#   Binance-Peg USDC (18 dec): 0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d
PAYMENT_TOKEN=0x55d398326f99059fF775485246999027B3197955

# ── Campaign Owner ────────────────────────────────────────────────────────────
# Multisig or EOA that will own all campaign contracts after deployment.
# Defaults to the deployer if left empty.
CAMPAIGN_OWNER=

# ── Token Config ──────────────────────────────────────────────────────────────
TOKEN_NAME=XProtocol
TOKEN_SYMBOL=XPC

# Hard cap on LaunchToken supply (18 decimals).
# Example: 1_000_000 tokens = 1000000000000000000000000
MAX_SUPPLY=1000000000000000000000000

# Tokens for sale in the auction (18 decimals). Must be <= MAX_SUPPLY.
TOTAL_SUPPLY=1000000000000000000000000

# ── Auction Timing ────────────────────────────────────────────────────────────
# How long the auction runs, in seconds. Default: 7 days = 604800
AUCTION_DURATION=604800

# Seconds between auction end and TGE unlock. Default: 30 days = 2592000
UNLOCK_DELAY=2592000

# ── Auction Pricing ───────────────────────────────────────────────────────────
# ⚠️  18 decimals. Floor price per LaunchToken in payment token units.
# Example: $1 USDT = 1000000000000000000 (1e18)
MIN_PRICE=1000000000000000000

# ── RT Cap ────────────────────────────────────────────────────────────────────
# Max TGE price as a multiple of clearingPrice. Must be > 1.
# Example: 5 means RT pays out on at most 5x the clearing price.
# Max USDC liability = totalRT * clearingPrice * (RT_CAP_MULTIPLIER - 1)
RT_CAP_MULTIPLIER=5
